pipeline {
    agent any
    environment {
        // Optionally, specify the DependencyCheck tool installation here if needed
        DEPENDENCY_CHECK_HOME = tool name: 'DependencyCheck', type: 'DependencyCheck'
    }
    stages {
        stage('Checkout SCM') {
            steps {
                // Add your checkout step here
                git branch: 'master', url: 'https://github.com/KaungMK/JenkinsDependencyCheckTest.git'
            }
        }
        stage('OWASP DependencyCheck') {
            steps {
                // Assuming you have a step to run DependencyCheck here
                dependencyCheck()
            }
        }
    }
}
